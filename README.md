# Criptografia.py
 Arquivo que tenta imitar login, a senha é criptografada com sha256, são gerados dois arquivos, um que contém os dados e outro que contrém os textos ou qualquer outra coisa que será adicionado.
